apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: data-prod
spec:
  ports:
    - port: 5432
  selector:
    app: user-db
  clusterIP: None
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-db
  namespace: data-prod
spec:
  serviceName: user-db
  replicas: 1
  selector:
    matchLabels:
      app: user-db
  template:
    metadata:
      labels:
        app: user-db
    spec:
      containers:
        - name: user-db-postgres
          image: docker.io/postgres:18-alpine
          env:
            - name: POSTGRES_DB
              value: user_db
            - name: POSTGRES_USER
              value: karga
            - name: POSTGRES_PASSWORD_FILE
              value: "/etc/secrets/user_db_password"
          volumeMounts:
            - name: user-db-data
              mountPath: /var/lib/postgresql
            - name: secret-volume
              mountPath: /etc/secrets
              readOnly: true
      volumes:
        - name: secret-volume
          secret:
            secretName: user-db-secret
  volumeClaimTemplates:
    - metadata:
        name: user-db-data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 2Gi

